@model MvcMovie.Models.HelloWorldModel

@{
    ViewData["Title"] = "Welcome";
}

<h1>Witaj</h1>

<p>Imiê: @Model.Name</p>
<p>Iloœæ powtórzeñ: @Model.NumTimes</p>

<ul>
    @for (int i = 0; i < Model.NumTimes; i++)
    {
        <li>Witaj @Model.Name!</li>
    }
</ul>
